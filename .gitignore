# secrets — never commit
.env

# sf-food-data.js is checked in so Vercel can serve it.
# Re-generate with: node generate-data.js

# node
node_modules/
npm-debug.log*

# os
.DS_Store

.env.local

# convex generated
convex/_generated/
.admin-key.local
